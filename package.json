{
  "name": "0verdistribution",
  "version": "1.0.0",
  "description": "各校下載分發學生資料",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "harp src dist && babel dist/js -d dist/js",
    "docker-build": "docker run -it --rm -v $PWD:/0verdistribution -w /0verdistribution node:22.20.0 sh -c 'npm install && npm run build' ",
    "serve": "harp src"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@babel/cli": "^7.29.7",
    "@babel/core": "^7.29.7",
    "@babel/preset-env": "^7.29.7",
    "@popperjs/core": "^2.11.8",
    "bootstrap": "^5.3.8",
    "harp": "^0.48.1"
  },
  "overrides": {
    "ejs": "^3.1.10",
    "send": "^0.19.0",
    "esbuild": "^0.25.0"
  }
}
